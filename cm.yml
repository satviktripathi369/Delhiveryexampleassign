apiVersion: v1
kind: ConfigMap
metadata:
  name: wordpress-css-config
data:
  custom.css: |
    body {
      background-color: #f5deb3; /* Wheat background for demo */
    }
    h1 {
      color: #4682b4; /* SteelBlue for demo */
    }
    button {
      background-color: #ff4500; /* OrangeRed buttons for demo */
      color: white;
    }

